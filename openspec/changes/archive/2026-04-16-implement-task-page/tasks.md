## 1. 建立任务领域模型

- [x] 1.1 定义长期任务模型字段，覆盖标题、等级、手动进度、创建时间、锁定截止时间与完成时间，不设置长期任务通用状态字段；展示层基于锁定截止时间计算剩余锁定时间
- [x] 1.2 定义日常任务模型字段，覆盖标题、状态、开始时间、结束时间、等级、所属长期任务与创建时间
- [x] 1.3 建立等级 `S / A / B / C` 与任务状态枚举
- [x] 1.4 更新 SwiftData schema 与预览/测试容器，包含长期任务与日常任务

## 2. 实现任务应用服务

- [x] 2.1 实现长期任务创建、读取、编辑、删除服务，并校验标题长度与等级
- [x] 2.2 实现长期任务当前专注、暂停任务库、已达成里程碑的分区推导，不依赖长期任务通用状态字段
- [x] 2.3 实现最多 3 个当前专注 + 暂停长期任务的限制
- [x] 2.4 实现日常任务创建、读取、编辑、删除服务，并强制选择一个所属长期任务；存在当前 TopOne 时默认选中当前 TopOne
- [x] 2.5 实现日常任务 `未开始 + 执行中` 最多 5 个、`执行中` 最多 2 个的限制
- [x] 2.6 实现日常任务状态切换，并在进入执行中/已完成时记录开始时间与结束时间
- [x] 2.7 实现日常任务默认排序：执行中、未开始、已完成

## 3. 实现 TopOne 进度与切换规则

- [x] 3.1 实现当前 TopOne 手动百分比进度更新，并限制在 `0...1`
- [x] 3.2 实现没有当前 TopOne 时设置新 TopOne 并选择锁定时间：7 天、14 天、30 天、自定义（最长 180 天）
- [x] 3.3 实现已有当前 TopOne 时必须先解绑，锁定期内解绑必须填写更换理由，解绑后才能选择其他长期任务为 TopOne
- [x] 3.4 实现更换理由最少字数门槛：初始 50、提前更换次数驱动翻倍、完成 TopOne 后将提前更换次数减半、范围 50 到 500
- [x] 3.5 实现更换理由最大 1000 字限制与差值反馈所需的领域计算

## 4. 接入任务列表页动态交互

- [x] 4.1 按 `ios-task-page.html` 重构任务页视觉结构：顶部 `TopOne` 导航栏、浅暖背景、深色当前专注 Hero 卡片、金色强调进度与充足留白
- [x] 4.2 实现任务页动态展示当前专注任务卡片，内容来自真实当前 TopOne，并确保当前 TopOne 是第一视觉锚点
- [x] 4.3 实现当前 TopOne 的日常任务列表；允许编辑和删除任意日常任务，但只有当前 TopOne 下的日常任务允许更新状态
- [x] 4.4 实现日常任务项视觉样式：执行中最醒目、未开始清晰但更轻、已完成明显弱化，避免多余标签和管理后台感
- [x] 4.5 实现暂停任务库，按 `S / A / B / C` 分组并默认折叠或弱化展示，视觉强度低于当前 TopOne
- [x] 4.6 实现已达成里程碑区，默认折叠展示已完成长期任务
- [x] 4.7 实现右下角圆形 `+` 浮动按钮进入统一创建任务页，页内切换新增长期任务 / 新增日常任务，不能只放在导航栏；按钮按屏幕尺寸动态适配右侧与底部间距
- [x] 4.8 实现长期任务添加、编辑、删除界面流程，并让创建页参考 `ios-goal-create.html` 的页面气质与输入结构
- [x] 4.9 实现日常任务添加、编辑、删除界面流程
- [x] 4.10 实现日常任务左侧勾选状态切换与 `swipeActions` 左滑 `更新 / 编辑 / 删除` 入口，其中更新入口仅对当前 TopOne 下的日常任务启用；不得用行内 `Menu` 替代左滑
- [x] 4.11 实现当前 TopOne 进度条滑动更新与点击百分比手动输入，视觉参考原型金色进度条与百分比层级
- [x] 4.12 实现参考 `ios-goal-commitment-lock.html` 的锁定时间选择弹层，包含 `7 天 / 14 天 / 30 天 / 自定义`
- [x] 4.13 实现参考 `ios-force-switch.html` 的提前切换理由页或全屏弹层，包含字数差值、规则说明、`再坚持一下` 与 `确定放弃`
- [x] 4.14 移除原型静态示例数据，确保页面内容由模型与状态驱动
- [x] 4.15 实现顶部栏下方的任务页精神性导语，并保持当前 TopOne Hero 作为任务内容最强视觉锚点
- [x] 4.16 实现底部任务 / 奖励 / 设置导航，奖励与设置仅作为后续页面占位，不实现正式管理功能
- [x] 4.17 实现长期任务 100% 完成后的轻量庆祝反馈，并将已完成长期任务释放出当前 TopOne 后进入里程碑区

## 5. 补充测试与验证

- [x] 5.1 增加长期任务创建、编辑、删除与标题/等级校验测试
- [x] 5.2 增加长期任务数量限制与唯一 TopOne 测试
- [x] 5.3 增加日常任务归属、数量限制、状态切换与排序测试
- [x] 5.4 增加 TopOne 进度边界测试
- [x] 5.5 增加锁定时间与提前切换理由门槛规则测试
- [x] 5.6 增加 UI 级验收检查：通过代码实现检查确认任务页不再是系统默认 Form/List/Menu 堆叠，关键页面与交互对齐 `docs/prototype/v1.0/screens`
- [x] 5.8 增加 UI 级验收检查：通过代码实现检查确认顶部导语、暂停任务库、已达成里程碑与长期任务分组层级符合最新原型要求，且长期任务条目无图标
- [x] 5.9 增加 UI 级验收检查：通过代码实现检查确认新增任务页不再保留系统表单感，左滑动作带的视觉与交互语义对齐 `ios-task-page-swipe-actions.html`
- [x] 5.10 增加 UI 级验收检查：确认底部导航贴合底部安全区，`+` 浮动按钮动态适配右下角位置
- [x] 5.7 运行 `format` / `lint` / `build` / `test` 并修复本 change 范围内的问题

## 6. 回写进度

- [x] 6.1 更新 `docs/compliance/v1.0/progress.md` 记录任务页动态实现推进状态
- [x] 6.2 实现完成后回写本 OpenSpec change 任务状态
